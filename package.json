{
  "name": "stremio-live-tv",
  "version": "1.1.1",
  "private": true,
  "type": "module",
  "description": "Private M3U live channels in Stremio Discover",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "start": "node --env-file-if-exists=.env src/server.mjs",
    "test": "node --test",
    "check": "node --check src/server.mjs && node --check public/app.js && node --check docs/guide.js && node scripts/sync-docs.mjs --check && node --test",
    "sync-docs": "node scripts/sync-docs.mjs"
  },
  "license": "MIT",
  "dependencies": {
    "saxes": "6.0.0"
  }
}
