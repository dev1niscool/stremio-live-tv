# Copy to .env for local use. Never commit your actual .env.
# Generate a key with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ADDON_TOKEN=
PLAYLISTS_JSON=[]
PORT=7860
# Set your public origin when using a reverse proxy. HF's SPACE_HOST is detected automatically.
# PUBLIC_URL=https://your-account-your-space.hf.space
# CACHE_TTL_SECONDS=900
